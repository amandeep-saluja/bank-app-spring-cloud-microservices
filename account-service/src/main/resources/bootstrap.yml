spring:
  application:
    name: AccountService
  config:
    import: optional:configserver:http://localhost:1111
  cloud:
    config:
      failFast: true
      
management:
  endpoints:
    web:
      exposure:
        include: "*"